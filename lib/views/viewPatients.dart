import 'package:flutter/cupertino.dart';

class ViewDetails extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("View all patients details"),
    );
  }
}
